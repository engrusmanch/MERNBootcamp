// conditonals, new operator (===,&&, ||, ??), arrays, loops
// Github 

if ((1<3) && (1<4)){ //AND operator
    // do something
}

if ((1<3) || (1<0)){ //OR operator
    // do something
}


let height = 0;
height = height ?? 1;

let variable; // variable declaration
variable = "some_value"; // vriable initialization

const prompt = require("prompt-sync")();
const num = prompt("Enter Number ");
if (num % 2 == 0) {
  console.log("The Number is Even");
}
else if (num % 7 == 0){
    console.log("number is divisble by 7");
}
else {
  console.log("It is Odd");
}

// looping structures 

let counter = 1;
while (toffe >= 0) {
    // code that to be run again and again and again and ...
    console("Sit");
    counter = counter + 1; // 1st approach
    conunter++; // 2nd way
    counter+= 1; // approach 
}

// arrays -- data is group toghether
const names = ["wasif", "naveed", "waqas", "hasseb"];

console.log(names[0].length); // 5

names[44] = "rafay";

names[names.length] = "dflkdf";

names.push("something else");
names.pop();

Math.random();

let computerChoice = choices[Math.floor(Math.random()*3)];


"iamstriing"


length = length -1;  // true 


let reversed_toffes = "";

// some while loop

{
    reversed_toffes += toffes[conter];
}

